<h1><%= t('resources.books.actions.edit', resource: Book.model_name.human) %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
   <%= link_to t('resources.books.actions.show', resource: Book.model_name.human), book_path(@book) %> |
   <%= link_to t('resources.books.actions.back_to_index', resource: Book.model_name.human), books_path %>
</nav>
